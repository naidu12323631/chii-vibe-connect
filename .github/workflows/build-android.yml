name: Build Android APK

# Builds the Android app and publishes the APK as a rolling "continuous" GitHub
# Release, so the landing-page download button can always point at
# https://github.com/<owner>/<repo>/releases/latest/download/milo.apk
on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-apk:
    runs-on: ubuntu-latest
    # Uses the Supabase secrets stored in the "supabase" GitHub environment.
    environment: supabase
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android platform + build tools
        run: |
          sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Check Supabase config
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}
          VITE_SUPABASE_PROJECT_ID: ${{ secrets.VITE_SUPABASE_PROJECT_ID }}
        run: |
          test -n "$VITE_SUPABASE_URL" \
            || echo "::error::MISSING secret: VITE_SUPABASE_URL"
          test -n "$VITE_SUPABASE_PUBLISHABLE_KEY" \
            || echo "::error::MISSING secret: VITE_SUPABASE_PUBLISHABLE_KEY"
          test -n "$VITE_SUPABASE_PROJECT_ID" \
            || echo "::error::MISSING secret: VITE_SUPABASE_PROJECT_ID"
          test -n "$VITE_SUPABASE_URL" && test -n "$VITE_SUPABASE_PUBLISHABLE_KEY" \
            || { echo "::error::Add the missing secrets under Settings -> Secrets and variables -> Actions -> Secrets (the Secrets tab, not Variables)."; exit 1; }

      - name: Build web app
        env:
          VITE_SUPABASE_PROJECT_ID: ${{ secrets.VITE_SUPABASE_PROJECT_ID }}
          VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
        run: npm run build

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Restore signing keystore
        id: keystore-cache
        uses: actions/cache@v4
        with:
          path: android/release.keystore
          key: milo-release-keystore-v1

      - name: Generate release keystore
        if: steps.keystore-cache.outputs.cache-hit != 'true'
        run: |
          "$JAVA_HOME/bin/keytool" -genkeypair -v \
            -keystore android/release.keystore \
            -alias milo -keyalg RSA -keysize 2048 -validity 10000 \
            -storepass "milo-release" -keypass "milo-release" \
            -dname "CN=milo, OU=milo, O=milo, L=City, S=State, C=US"

      - name: Build release APK
        run: ./gradlew assembleRelease
        working-directory: android

      - name: Rename APK
        run: mv android/app/build/outputs/apk/release/app-release.apk milo.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: milo-apk
          path: milo.apk

      - name: Publish to GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release view continuous >/dev/null 2>&1 && gh release delete continuous --yes || true
          gh release create continuous milo.apk \
            --title "milo for Android" \
            --latest \
            --notes "Latest Android build of milo. Download the APK and install it on your device."
